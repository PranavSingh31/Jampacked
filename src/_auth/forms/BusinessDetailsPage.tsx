// import { useForm } from "react-hook-form";
// import { zodResolver } from "@hookform/resolvers/zod";
// import { SignupValidation } from "@/lib/validation"; // Assuming this is the path to your validation schema

// const BusinessDetailsPage = () => {
//     const form = useForm({
//         resolver: zodResolver(GoogleSignupValidation),
//         // other form options
//     });

//     const onSubmit = (data) => {
//         // Handle business details submission
//     };

//     return (
//         <form onSubmit={form.handleSubmit(onSubmit)}>
//             <input {...form.register('businessname')} placeholder="Business Name" />
//             <input {...form.register('location')} placeholder="Location" />
//             <button type="submit">Save Details</button>
//         </form>
//     );
// };

// export default BusinessDetailsPage;
