export type INavLink = {
    imgURL: string;
    route: string;
    label: string;
};

export type INewUser = {
    businessname: string;
    location: string;
    number: string;
    email: string;
    password: string;
};